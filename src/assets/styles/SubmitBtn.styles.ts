import { makeStyles } from "@material-ui/core/styles";

const useSubmitBtnStyle = makeStyles({
    button : {
        margin: '2.5rem',
        border :'1px solid rgba(255, 255, 255, .5)',
        borderRadius : '1rem',
        padding :'.5rem 1rem',
        background : 'inherit'
    }
});

export default useSubmitBtnStyle;